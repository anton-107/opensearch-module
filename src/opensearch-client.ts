export class OpenSearchClient {
  public async addToIndex(
    documentID: string,
    document: Record<string, string>
  ) {
    // eslint-disable-next-line no-console
    console.log(
      "Not implemented. Was requested to add to index",
      documentID,
      document
    );
  }
}
